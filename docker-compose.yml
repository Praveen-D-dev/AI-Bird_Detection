version: '3.8'

services:
  # 1. Express MERN Backend (Port 5000)
  node-backend:
    build: ./backend-node
    container_name: bird-node-backend
    restart: always
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGODB_URI=mongodb+srv://praveen:pYpJdWEV32jOowc6@cluster0.l9u16m5.mongodb.net/bird_deterrent?retryWrites=true&w=majority&appName=Cluster0
      - PYTHON_BACKEND_URL=http://python-ai:5001
    depends_on:
      - python-ai

  # 2. Python YOLO AI Service (Port 5001)
  python-ai:
    build: ./backend
    container_name: bird-python-ai
    restart: always
    ports:
      - "5001:5001"
    environment:
      - PORT=5001
